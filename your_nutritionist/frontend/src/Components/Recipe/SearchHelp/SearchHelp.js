import React from 'react'
import { Modal, Button, Form, Row, Col } from 'react-bootstrap';

const SearchHelp = (props) => {
    return <>
        <p>To search for ingredient, add + before the </p>
    </>
}

export default SearchHelp